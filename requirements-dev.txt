# CI / local dev tooling — pinned so the gate is deterministic. A floating `pip install
# ruff` meant a new ruff release could reformat rules and fail `ruff format --check` on code
# nobody touched; pinning turns tool drift into an intentional, Dependabot-reviewable bump.
# Dependabot's pip ecosystem already scans requirements*.txt, so these get PRs like any dep.
ruff==0.16.5
pytest==9.1.1
httpx==0.28.1
pip-audit==2.10.1
